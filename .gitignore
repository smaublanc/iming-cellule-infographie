# Ce depot suit le site (maquette/ + Portfolio/) ainsi que les scripts
# d'automatisation et les ressources de marque -- tout ce qu'il faut pour
# reprendre le travail depuis une autre machine (voir README.md).
# Exclus : etat WordPress/Local (specifique a cette machine, voir README),
# fichiers generes/temporaires.

wp-*.php
mysqld-manual.log
portfolio-inventory.json
*.log
Thumbs.db
desktop.ini
_gsdata_/
link-check-result.txt
